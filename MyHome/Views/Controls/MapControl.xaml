<UserControl
    x:Class="MyHome.Views.MapControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:my="using:Windows.UI.Xaml.Controls.Maps"
    d:DesignHeight="1080"
    d:DesignWidth="1920"
    mc:Ignorable="d">
    <Grid Margin="20">
        <my:MapControl
            x:Name="MyMapControl"
            Center="{x:Bind ViewModel.HomeLocation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            DesiredPitch="30"
            Layers="{x:Bind ViewModel.Layers, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ZoomLevel="17" />
    </Grid>
</UserControl>
